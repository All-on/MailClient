# MailClient
This project is a cross-platform email client (user agent) that supports sending and receiving emails via SMTP and POP3 protocols, and implements support for MIME, secure encryption, key negotiation, and robustness under unreliable server conditions.
